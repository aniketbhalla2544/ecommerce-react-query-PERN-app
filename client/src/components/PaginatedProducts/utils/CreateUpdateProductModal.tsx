type ThisProps = {
  title?: string;
};

const CreateUpdateProductModal = ({ title }: ThisProps) => {
  return <div>CreateUpdateProductModal {title}</div>;
};

export default CreateUpdateProductModal;
